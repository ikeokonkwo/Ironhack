<h1> Welcome to Music Concert Network </h1>
<br>
<br>
	<ul>
	<% @my_event.each do |event| %>
		<li>
			<%= event.artist %>
			<% if event.description.present? %>
			-<%= link_to "See Details", "/events/#{event.id}"%>
			<%end%>
		</li>
		<%end%>
	</ul>

<br>
<%= link_to "Add a concert", new_event_path%>
